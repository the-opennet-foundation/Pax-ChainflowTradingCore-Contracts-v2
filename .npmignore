# Standard ignores
node_modules/
coverage/
.env
.DS_Store
*.log

# Ignore development and test files
src/ # If source files are in a 'src' folder but you only want to publish 'contracts' and 'artifacts'
docs/
test/
tests/
spec/

# Ignore configuration and development setup files
.github/
.vscode/
.idea/
.prettierignore
.gitignore
.eslintrc*
hardhat.config.*
truffle-config.*
foundry.toml
package-lock.json
yarn.lock
tsconfig.json
jest.config.*
mocharc.*

# Ignore compilation artifacts and cache
artifacts/
cache/
build/
dist/
**/autogenerated/*

# Exclude all source files by default, then selectively include necessary ones
**/*.sol
**/*.ts
**/*.js
**/*.css
**/*.scss
**/*.html

# Re-include essential files/directories (adjust based on your project structure)
# This is crucial: npm uses a 'files' field in package.json or explicit negations in .npmignore
!/contracts/**/*.sol
!/artifacts/**/*.json
!/build/**/*.json
!/dist/**/*.json
!/index.js
!/index.ts