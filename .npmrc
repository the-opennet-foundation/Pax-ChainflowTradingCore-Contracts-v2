# Enable automatic peer dependency installation (npm v7+ behavior)
auto-install-peers=true
# Use with caution: set to true to ignore peer dependency conflicts (legacy behavior)
legacy-peer-deps=false 
